{
  "name": "hw2-decoupling-visualization",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "prepare-data": "node scripts/prepare-data.mjs",
    "prepare-static": "node scripts/prepare-static.mjs",
    "dev": "vite",
    "build": "npm run prepare-data && npm run prepare-static && vue-tsc -b && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"

  },
  "dependencies": {
    "d3": "^7.9.0",
    "vue": "^3.5.30"
  },
  "devDependencies": {
    "@types/d3": "^7.4.3",
    "@types/node": "^24.12.0",
    "@vitejs/plugin-vue": "^6.0.5",
    "@vue/tsconfig": "^0.9.0",
    "typescript": "~5.9.3",
    "vite": "^8.0.1",
    "vue-tsc": "^3.2.5"
  }
}
